{
  "name": "promptcanvas",
  "version": "1.0.0",
  "description": "키워드로 트리거되는 동적 JSON 프롬프트 생성기",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "rimraf": "^5.0.0"
  }
}
